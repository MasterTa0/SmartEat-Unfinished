一个关于利用并结合ai和机器学习让用户通过拍照的方式获取照片中的饮食大致重量、大致的营养成分(通过照片计算体积,识别食物种类),再让用户以文本形式填充拍照没有获取的信息以更好地分析饮食的各种信息后,更新用户每周每月的饮食任务,并不断调整的手机软件

##本地运行
*先决条件:**Node.js

1.安装依赖项:
   `npm install`
   
2.在.env.local中设置`GEMINI_API_KEY` in [.env.local](.env.local) 为您的Gemini API密钥

3.运行应用:
   `npm run dev`
